/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StartDateStatusDirective } from './start-date-status.directive';

describe('StartDateStatusDirective', () => {
  it('should create an instance', () => {
    let directive = new StartDateStatusDirective();
    expect(directive).toBeTruthy();
  });
});
